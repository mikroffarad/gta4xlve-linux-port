# This tablet's WCN3991 never answers the IBS wake-up that follows a
# host-initiated sleep: the controller goes silent, every command then times
# out, and the driver crash-dumps and re-initialises it in a loop. Keep it
# awake instead.
options hci_uart ibs_host_sleep=0
